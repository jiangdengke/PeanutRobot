# Task

更新时间：2026-06-01

## 已完成

- [x] 项目结构梳理。
- [x] 更新 `README.md`，补充构建、运行、接口、WebSocket 和注意事项。
- [x] 新增 `plan.md`，记录阶段性技术计划。
- [x] 新增 `task.md`，记录当前任务和后续 backlog。
- [x] 确认当前应用版本为 `1.0.1`，运行包名为 `com.keenon.zealq`。
- [x] 确认本机 HTTP 服务端口为 `9095`。
- [x] 确认状态上报 WebSocket 为 `ws://192.168.112.194:9096`。
- [x] 确认仓库任务 WebSocket 为 `ws://192.168.112.194:9098`。

## P0：优先修复

- [ ] 抽出硬编码配置：SDK `appId`、`secret`、WebSocket 地址、机器人 ID、任务 ID、HTTP 端口。
- [ ] 修复未选择点位时“立即出发”按钮不会恢复可点击的问题。
- [ ] 将点位选中状态从 View 状态迁移到数据状态，避免 RecyclerView 复用导致选择异常。
- [ ] 为 `/robot_task/go_to_charge`、`/robot_task/send_point`、`/robot_task/send_stop` 增加模块初始化检查。
- [ ] 为 HTTP 请求增加参数校验，空 body、非法 JSON、字段缺失时返回明确错误。
- [ ] 明确 Peanut SDK 初始化失败时的 UI 提示和重试入口。

## P1：稳定性和可维护性

- [ ] 统一 HTTP 成功和失败响应格式。
- [ ] 给 WebSocket 上报增加连接状态记录和发送失败处理。
- [ ] 梳理 `MainActivity`，拆出导航控制、锁屏控制、副屏控制、HTTP 命令处理。
- [ ] 清理重复依赖：Material、Java-WebSocket、Kotlin Gradle 插件。
- [ ] 评估 `jcenter()` 依赖来源，能迁移的迁移到 `mavenCentral()` 或明确保留原因。
- [ ] 为视频上传增加文件类型、大小和保存失败处理。
- [ ] 为副屏图片轮播增加空列表保护。

## P2：交付增强

- [ ] 增加现场配置页，用于修改服务器地址、任务 ID、速度和锁屏参数。
- [ ] 增加接口 smoke test 脚本，覆盖点位列表、导航、停止、速度配置、屏幕控制。
- [ ] 增加发布前检查脚本，校验版本号、包名、AAR、Gradle 构建和 Release 文件名。
- [ ] 增加正式签名构建流程。
- [ ] 补充现场验收清单，包括权限、SDK 授权、点位读取、导航、回充、副屏、WebSocket 上报。

## 验证清单

- [ ] `./gradlew :app:assembleDebug` 构建通过。
- [ ] 安装到目标机器人后权限授权正常。
- [ ] 启动后 Peanut SDK 初始化成功。
- [ ] 点位列表可自动加载，手动刷新可用。
- [ ] 单点和多点导航速度配置生效。
- [ ] 本机 `9095` HTTP 接口可被局域网服务调用。
- [ ] 状态上报 WebSocket 可持续发送。
- [ ] 回充和巡仓按钮发送的 JSON 与业务系统约定一致。
- [ ] 空闲锁屏、解锁、屏保图片配置可用。
- [ ] 副屏视频和图片轮播在双屏设备上可用。
