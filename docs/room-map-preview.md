# 房间点位对照图

## 图片放置位置

将房间点位对照图放到以下路径：

```text
app/src/main/res/drawable/room_map.png
```

要求：

- 文件名必须是 `room_map.png`。
- 文件名不要使用中文、空格或大写字母。
- 替换图片后需要重新构建并安装 App。

## 使用方式

主界面左下角有“展示图片”按钮。

- 点击“展示图片”后，会显示房间点位对照图。
- 点击右上角 `X` 关闭图片。
- 如果图片还没有放到指定路径，App 会提示需要先放置图片。
